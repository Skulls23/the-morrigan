using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FootValidator : MonoBehaviour
{
    /*public PlayerSoundManager PSManager;
    public bool AllowedToCheck = false;

    private void Start()
    {
        PSManager = PSManager.GetComponent<PlayerSoundManager>();
    }

    private void OnTriggerEnter(Collider other)
    {
        Debug.Log("Test");
        if (other.gameObject.CompareTag("Ground"))
        {
            PSManager.PlayFootstep();
        }
    }

    /*private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("Ground"))
        {
            AllowedToCheck = true;
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.gameObject.CompareTag("Ground"))
        {
            AllowedToCheck = false;
        }
    }*/
}
